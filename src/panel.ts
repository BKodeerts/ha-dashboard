/**
 * Entry point for the Home Assistant custom panel build.
 *
 * HA loads this module and instantiates `<ha-dashboard-panel>`, setting `hass`,
 * `narrow`, `route` and `panel` on it. See README.md for the `panel_custom`
 * snippet that registers it.
 */
import './element';
